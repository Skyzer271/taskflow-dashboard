# GitHub Pages deployment is disabled because the app is hosted on Fly.io.
# This workflow only runs manually and does nothing.

name: Deploy frontend to GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: echo "GitHub Pages deployment is disabled. The app is deployed via Fly.io (see .github/workflows/fly.yml)."
